import { Typography } from "@mui/material";

export default function UserManagementPage() {
  return (
    <div>
      <Typography variant="h5" gutterBottom>
        User management
      </Typography>
      <Typography color="text.secondary">
        Placeholder. Add CRUD UI when your API is wired.
      </Typography>
    </div>
  );
}
