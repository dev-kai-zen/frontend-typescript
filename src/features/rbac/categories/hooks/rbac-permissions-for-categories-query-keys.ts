/** Full permissions list used on the categories page for per-category code counts. */
export const rbacPermissionsForCategoriesKeys = {
  all: ["rbac", "permissions", "list-for-categories"] as const,
};
